# Task Manager

Simple accessible task manager web app.

How to run

Open `index.html` in your browser (no server required):

```bash
# on Windows, macOS, Linux
start index.html  # or open the file in a browser
```

Features

- Add tasks
- Mark tasks completed
- Delete tasks
- Persists in `localStorage`
